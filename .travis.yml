language: android

jdk:
 - oraclejdk7
 - oraclejdk8

android:
  components:
    - build-tools-22.0.1
    - android-22

script:
  - ./gradlew clean check -Dpre-dex=false

cache:
  directories:
    - $HOME/.gradle
