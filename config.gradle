ext {
    def major = 6
    def minor = 0
    def patch = 0

    buildConfig = [
            "minSdk"    : 24,
            "compileSdk": 37,
            "targetSdk" : 37,
            "buildTools": "35.0.0"
    ]
    releaseConfig = [
            "group"      : "com.infinum.dbinspector",
            "version"    : "$major.$minor.$patch",
            "versionCode": major * 100 * 100 + minor * 100 + patch
    ]
}