ext {
    def major = 5
    def minor = 5
    def patch = 0

    buildConfig = [
            "minSdk"    : 21,
            "compileSdk": 34,
            "targetSdk" : 34,
            "buildTools": "34.0.0"
    ]
    releaseConfig = [
            "group"      : "com.infinum.dbinspector",
            "version"    : "$major.$minor.$patch",
            "versionCode": major * 100 * 100 + minor * 100 + patch
    ]
}